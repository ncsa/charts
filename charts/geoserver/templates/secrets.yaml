apiVersion: v1
kind: Secret
metadata:
  name: {{ include "geoserver.fullname" . }}
  labels:
    {{- include "geoserver.labels" . | nindent 4 }}
data:
  GEOSERVER_ADMIN_USER: {{ .Values.username | b64enc | quote }}
  GEOSERVER_ADMIN_PASSWORD: {{ .Values.password | b64enc | quote }}
